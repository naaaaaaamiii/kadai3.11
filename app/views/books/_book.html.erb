
          <h2 class="mt-3">New book</h2>
            <div class="row">
            <%= form_with model: book_new do |f| %>
             <p>Title</p>
              <%= f.text_field :title %>
   　         <p>Option</p>
              <%= f.text_area :body %><br>
              <%= f.submit 'Create Book' %>
            <% end %>
            </div>